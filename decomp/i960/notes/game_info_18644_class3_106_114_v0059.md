# fa_game_info 0x18644: exact 0x106 ↔ 0x114 recovery (v0059)

## Corridor

- `0x106 = bit8 | bit1 | bit2`
- `0x114 = bit8 | bit2 | bit4`
- vector class: `3`
- ROM vector: `(208, 213, 205, 210)`

## Semantic probe

After admitting only the bilateral `0x106 ↔ 0x114` composition, all eight orientation/countdown/mode scenarios matched architecturally. The only differences were instruction accounting.

Measured semantic-only deltas, candidate minus ROM:

### `0x106 -> 0x114`

- first call (`0x164b0`): `+7` without countdown, `-4` with countdown
- second call (`0x164c4`): `+3/+2` without countdown (`mode6=0/1`), `-14` with countdown

### `0x114 -> 0x106`

- first call (`0x164b0`): `-1` without countdown, `-15` with countdown
- second call (`0x164c4`): `+11/+10` without countdown (`mode6=0/1`), `-3` with countdown

When normalized by the actual child-call orientation (`r7/r8`, which reverses on the second child), these are exactly the already recovered class-3 accounting formulas used by `0x102 ↔ 0x114` and `0x106 ↔ 0x110`. The existing predicate was widened; no third accounting family was introduced.

## Validation

With measured accounting applied:

- isolated native-child checkpoints: **16/16 exact**
- full native child #1 -> caller ROM -> native child #2 -> ROM tail chains: **8/8 exact**
- CPU/register architecture: exact
- mutable memory: exact
- `instructions`, `calls`, `returns`, `interrupt_entries`, `interrupt_returns`: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `24a5eaddb3485ca25604615605d0b5bd52ae4d2f`.

Matrix update commit: `838418d73edfb95a596b8ff6823a561fddcd304e`.

After promotion, the state-pair matrix contains **60/64 exact pairs**. All class-3 ordered pairs are now exact; only the two class-7 corridors remain unrecovered.
