# v0311: coli helper `0x230d4` bit-26 compact path

## Verdict

The long `0x225cc` body calls `0x230d4`. The measured **bit-26-set**
path is native:

```text
r4 = (i16)(g7+0x82a) + (i16)(g7+0x26) - (i16)(g8+0x5b4) + (1<<14)
table = (g8+0x142 bit 15) ? 0x0201cc48 : 0x0201cc54
r3++ when the r4 bit-15 polarity selects the other slot
g0 = *(u32*)(table + r3*4)
```

Measured shape (`+0x142` bit 15 clear, r4 bit 15 clear): body **15**,
0 calls, 1 return, `g0 = 0x00004421` from main-data `0x0201cc54`.

Bit 26 clear (long path at `0x2312c`) remains fail-closed.

## Drive

Park at `0x230d4` from `out/coli-midbody-22210.vf2snap` with the
both-hit mutations, then:

```text
--set-u32 0x00512b24=0x04000102   # f1+0x1a4 bit 26 set
--until 0x00023234
```

16 instructions including ret.

## Proof

- unit test `test_coli_230d4_bit26`: `16/0/1`, `g0 = 0x00004421`;
  bit 26 clear fails closed
- ctest Debug **56/56**
- PUNCH **320/320 MATCH**
- input-17 **64/64 MATCH**
