# fa_game_info 0x18644: exact 0x104 ↔ 0x112 recovery (v0055)

## Corridor

- `0x104 = bit8 | bit2`
- `0x112 = bit8 | bit1 | bit4`
- vector class: `3`
- ROM vector: `(208, 213, 205, 210)`

## Semantic probe

After admitting only the bilateral `0x104 ↔ 0x112` composition, all isolated cases matched architecturally. Remaining differences were instruction accounting only.

Measured semantic-only deltas, candidate minus ROM:

### `0x104 -> 0x112`

- first call (`0x164b0`): `-4` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `+14/+13` for countdown 0 (`mode6=0/1`), `-3` with countdown 1

### `0x112 -> 0x104`

- first call (`0x164b0`): `+10` with countdown 0, `-4` with countdown 1
- second call (`0x164c4`): `0/-1` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

These deltas are identical to the recovered `0x100 ↔ 0x112` class-3 corridor, so the same orientation-sensitive accounting family applies. The second caller invocation presents the pair with reversed `r7/r8`.

## Validation

With measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- mutable memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `af01b0d5fafcd347d89d978b27122d5596b2294d`.

Matrix update commit: `bf535ce484c5f285b488790af6048a7eeb48a92e`.

After promotion, the state-pair matrix contains **52/64 exact pairs**.
