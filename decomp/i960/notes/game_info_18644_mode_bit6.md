# fa_game_info `0x18644` mode bit 6 (`base+0x3351`)

The ROM tail at `0x18898..0x188ac` uses `bbc 6`, so nonzero values with bit 6 clear are not a separate branch. A controlled `base+0x3351 = 0x01` probe matches the zero baseline at 678 instructions for the full task.

With `base+0x3351 = 0x40` and fighter bit 29 clear, the ROM executes the extra `ld (g8),r15` / `bbs 29,r15` pair and rejoins the normal tail. The full task measures 683 instructions. Isolated `0x18644` entry/return probes measure 102 -> 105 instructions for the first invocation and 97 -> 99 for the swapped second invocation, explaining the +5 total. This recovery covers that neutral bit-6-set corridor; bit29-set and combinations with the already-special state/countdown branches remain explicit unsupported boundaries.

A controlled state-bit-8 probe closes the `r7 = 0`, `r8 = 0x00000100` corridor with mode bit 6 set. The mode-clear control matches at 684 instructions; the mode-bit-6 variant matches at 689. At the two `0x18644` invocations the ROM measures 101 -> 104 instructions for the first fighter order and 97 -> 99 after the caller swaps the fighters. The bit-8 exact-state formula therefore uses the bit-8 priority count only on the first order and rejoins two instructions earlier than the generic exact-state formula there.

The same controlled fifth-dispatch snapshot was then expanded into a mode-bit-6 + fighter1-bit-8 state matrix. Exact ROM/native matches were obtained for isolated bit 8 (689 instructions), plus bit 6 (876), bit 14 (870), bit 15 (679), bit 16 (877), bit 21 (691), bit 26 (680), bit 29 (690), bit 30 (689), and the priority pairs bit 14 + bit 15 (869) and bit 15 + bit 16 (876). The single-extra-state cases use the same two-instruction earlier rejoin; the two measured priority pairs already match the generic exact-state count.

The isolated state-bit-8 + bit-1 corridor (`r7 = 0`, `r8 = 0x00000102`) is now recovered through the `0x188cc..0x18978` sub-tree. The ROM first checks fighter1 type 24, then signed distance from fighter0 against `+/-0x1554`, then the mirrored fighter1 distance against `+/-0x6aac`; the deep path masks fighter1 `+0x844` with `0x03ff8000`, conditionally clearing bits 22/23, 18/21/19 and 20 at the three `r9` thresholds before ORing the result into `r10`. Relative to ordinary state bit 8, the four recovered exits add 2 instructions for type 24, 11 when the first distance misses, 20 when the second distance misses, and 30 plus the executed threshold-clear instructions on the deep path.

Controlled full-task differentials match exactly for mode bit 6 natural state (700 instructions), mode bit 6 clear (695), the type-24 early exit (691), and the first-distance-exact/second-distance-miss path (744). Direct `0x18644` pairwise probes also show that the deep bit-1 tree adds exactly +30 instructions with the countdown fast path and +32 on the measured non-countdown mirrored-distance path, matching the ROM even though those synthetic deep inputs expose a pre-existing absolute accounting discrepancy in the underlying bit-8 distance corridor itself. The caller's swapped fighter order is handled separately: mode-clear accounting recognizes the isolated state in `r7`, while mode-bit-6 continues through the already-recovered neutral second invocation.

The isolated state-bit-8 + bit-4 fast path is also recovered for zero countdown. At `0x18778..0x18784`, the ROM masks `r8` with `0x0401c010`; bit 4 therefore branches directly to the shared `0x18890` tail instead of entering the signed-distance tree. The mode-clear task already matched this path at 671 instructions. With mode bit 6 set, the first fighter order uses the same recovered fast path and the swapped second order (`r7 = 0x00000110`, `r8 = 0`) rejoins eight instructions earlier than the generic mode-bit-6 fallback accounting. The resulting full task matches the ROM exactly at 676 instructions, with the same 12 calls and 13 returns.

The isolated bilateral state-bit-8 corridor is recovered for both zero and nonzero countdown: both fighters have exactly `0x00000100`. With zero countdown, the two `0x18644` invocations are semantically distinguished by their return sites even though `r7` and `r8` are identical in both orders. With mode bit 6 clear the ROM measures 102 instructions for the first call (`return 0x164b0`) and 97 for the second (`return 0x164c4`), producing a 690-instruction full-task match. With mode bit 6 set those calls measure 105 and 99, producing an exact 695-instruction match. Relative to the unilateral state-bit-8 accounting, bilateral bit 8 adds one instruction only in the first fighter order; the swapped mode-bit-6 call rejoins one instruction earlier than the generic fallback.

With the countdown byte at `0x0050a0b6` nonzero, the bilateral calls shorten by exactly 13 instructions in every measured order/mode combination: mode-clear calls are 89 and 84 instructions, while mode-bit-6 calls are 92 and 86. The existing countdown fallback already represents that shared `0x18890` rejoin; only the first fighter order needs a one-instruction earlier bilateral adjustment. Complete ROM/native task snapshots match at 666 instructions with mode bit 6 clear and 671 with it set, including CPU state, mutable memory and call/return accounting.

The bilateral recovery remains deliberately limited to the isolated `0x00000100`/`0x00000100` state. Bilateral states carrying extra bits remain fail-closed. The bit-4 recovery likewise remains bounded to its measured zero-countdown corridor; mixed state8+bit1 and other unmeasured state combinations must be recovered independently rather than inferred from these exact cases.
