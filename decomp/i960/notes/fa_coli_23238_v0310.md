# v0310: coli helper `0x23238` early-out

## Verdict

The long `0x225cc` body calls `0x23238` twice. The measured early-out
is native: when `g0 != 0x2ce` the helper returns after `lda`/`cmpobne`
(body **2**, 0 calls, 1 return, `g0` unchanged).

The `g0 == 0x2ce` float-threshold path (`g8+0x1f8` vs `0.9` / `0.6`)
remains fail-closed.

## Shape

```text
0x23238  lda 0x2ce, r13
0x2323c  cmpobne r13, g0, ret   # taken when g0 != 0x2ce
# unmeasured:
0x23240  ld 0x1f8(g8), r3       # float
0x23244  lda 0x3f666666, r4     # ~0.9
         cmpr / bg ret
0x23254  lda 0x3f19999a, r4     # ~0.6
0x2325c  lda 0xa7, g0
         cmpr / bg ret
0x23268  lda 0x2cf, g0
0x2326c  ret
```

Measured from the long-`0x225cc` drive (`out/v305/both-nonzero-full.jsonl`):
both invocations took the `cmpobne` early-out.

## Proof

- unit test `test_coli_23238_early_out`: `3/0/1`, `g0` preserved;
  `g0 == 0x2ce` fails closed
- ctest Debug **56/56**
- PUNCH **320/320 MATCH**
- input-17 **64/64 MATCH**
- long `0x225cc` parent remains fail-closed
