# fa_game_info 0x18644: exact 0x110 ↔ 0x116 recovery (v0050)

## Corridor

- `0x110 = bit8 | bit4`
- `0x116 = bit8 | bit1 | bit2 | bit4`
- vector class: `5`
- ROM vector: `(213, 218, 213, 218)`

## Semantic probe

After admitting only the bilateral `0x110 ↔ 0x116` composition, all isolated cases matched architecturally. The remaining differences were instruction accounting only.

Measured semantic-only deltas, candidate minus ROM:

### `0x110 -> 0x116`

- first call (`0x164b0`): `-5` with countdown 0, `-19` with countdown 1
- second call (`0x164c4`): `+10/+9` for countdown 0 (`mode6=0/1`), `-7` with countdown 1

### `0x116 -> 0x110`

- first call (`0x164b0`): `+6` with countdown 0, `-8` with countdown 1
- second call (`0x164c4`): `-1/-2` for countdown 0 (`mode6=0/1`), `-18` with countdown 1

These deltas are exactly the class-5 accounting patterns already recovered for `0x114 ↔ 0x116`. The caller's second invocation presents the pair in reversed `r7/r8` order, so the accounting predicate is keyed to the actual child-call orientation.

## Validation

With the measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `a4a177b4c717187e7dacffe17e8804b42addd55c`.

After promotion, the state-pair matrix contains **42/64 exact pairs**.
