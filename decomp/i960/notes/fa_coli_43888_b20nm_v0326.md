# v0326: coli 0x43888 bit-20 non-match

## Verdict

`0x43888` runtime bit 20 set with `(g0 & 0x00ff0000) != 0x009e0000`
takes `cmpobne` to the store path without the `shlo`/`subo` subtract.
Unit **307**, ring `0x504020` stores the unmodified g0.

## Proof

- unit `test_coli_225cc_long`: bit20-nonmatch 307, `g0==0x00012345`
- PUNCH **320/320 MATCH**
- input-17 **64/64 MATCH**
- ctest Debug **56/56**

## Open

- `r11 >= 40` / `cmpoble 30`
- bit 4 / bit 26 at `0x22c8c`
- `g7+0x828` bits 10 / 8
