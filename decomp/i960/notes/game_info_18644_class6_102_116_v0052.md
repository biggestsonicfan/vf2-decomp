# fa_game_info 0x18644: exact 0x102 ↔ 0x116 recovery (v0052)

## Corridor

- `0x102 = bit8 | bit1`
- `0x116 = bit8 | bit1 | bit2 | bit4`
- vector class: `6`
- ROM vector: `(219, 224, 216, 221)`

## Semantic probe

After admitting only the bilateral `0x102 ↔ 0x116` composition, all isolated cases matched architecturally. The remaining differences were instruction accounting only.

Measured semantic-only deltas, candidate minus ROM:

### `0x102 -> 0x116`

- first call (`0x164b0`): `-4` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `+3/+2` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

### `0x116 -> 0x102`

- first call (`0x164b0`): `-1` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `0/-1` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

These deltas are exactly the existing class-6 accounting patterns. The caller's second invocation presents the pair in reversed `r7/r8` order, so accounting is keyed to the actual child-call orientation.

## Validation

With measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `59ed19462b444e7be771f36a5f6e591c47946e67`.

After promotion, the state-pair matrix contains **46/64 exact pairs**.
