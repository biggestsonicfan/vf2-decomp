# fa_game_info 0x18644: exact 0x100 ↔ 0x106 recovery (v0060)

## Corridor

- `0x100 = bit8`
- `0x106 = bit8 | bit1 | bit2`
- vector class: `7`
- ROM vector: `(221, 226, 197, 202)`

## Semantic probe

After admitting only the bilateral `0x100 ↔ 0x106` composition, every isolated case matched architecturally. All non-instruction counters were already exact; only instruction accounting differed.

Measured semantic-only deltas, candidate minus ROM:

### `0x100 -> 0x106`

- first call (`0x164b0`): `-9` without countdown, `-11` with countdown
- second call (`0x164c4`): `+6/+5` without countdown (`mode6=0/1`), `+1` with countdown

### `0x106 -> 0x100`

- first call (`0x164b0`): `+2` without countdown, `0` with countdown
- second call (`0x164c4`): `-5/-6` without countdown (`mode6=0/1`), `-10` with countdown

After normalizing for the actual child orientation (the second child sees `r7/r8` reversed), the accounting reuses two existing exact families:

- actual `0x100 -> 0x106`: the high class-7 family already used by `0x102 ↔ 0x106` (`+9/+11` on the first return and `+5/+6/+10` on the second);
- actual `0x106 -> 0x100`: the low bit2-family accounting (`-2` on the first no-countdown return; `-6/-5` no-countdown and `-1` countdown on the second return).

No new accounting formula was introduced; only the existing orientation-sensitive predicates were widened.

## Validation

With measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- mutable memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `ec3f1fa0f89be57fd0e255b95eb4e4f20f84eb00`.

Matrix update commit: `8223385991aa24db531309c14ddd9155dc8b784d`.

After promotion, the state-pair matrix contains **62/64 exact pairs**. The only unrecovered corridor is `0x104 ↔ 0x106`, also vector class 7.
