# fa_game_info 0x18644: exact 0x102 ↔ 0x114 recovery (v0054)

## Corridor

- `0x102 = bit8 | bit1`
- `0x114 = bit8 | bit2 | bit4`
- vector class: `3`
- ROM vector: `(208, 213, 205, 210)`

## Semantic probe

After admitting only the bilateral `0x102 ↔ 0x114` composition, every isolated case matched architecturally. The remaining differences were instruction accounting only.

Measured semantic-only deltas, candidate minus ROM:

### `0x102 -> 0x114`

- first call (`0x164b0`): `+7` with countdown 0, `-4` with countdown 1
- second call (`0x164c4`): `+3/+2` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

### `0x114 -> 0x102`

- first call (`0x164b0`): `-1` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `+11/+10` for countdown 0 (`mode6=0/1`), `-3` with countdown 1

The measured corrections reuse two already recovered orientation-sensitive class-3 accounting families. The actual child orientation `0x102 -> 0x114` uses `+4/-7` on the first return and `+3/-10/-11` on the second; the reverse child orientation uses `+15/+1` and `+14/-2/-3`. The caller's second invocation presents the pair with reversed `r7/r8`.

## Validation

With measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- mutable memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `29caa0d81ad785a5d2e52307c00f4f9a94dba046`.

Matrix update commit: `98fbd0d065badf4764856048a73cc86e814a1f9e`.

After promotion, the state-pair matrix contains **50/64 exact pairs**.
