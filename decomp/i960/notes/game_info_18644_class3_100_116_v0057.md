# fa_game_info 0x18644: exact 0x100 ↔ 0x116 recovery (v0057)

## Corridor

- `0x100 = bit8`
- `0x116 = bit8 | bit1 | bit2 | bit4`
- vector class: `3`
- ROM vector: `(208, 213, 205, 210)`

## Semantic probe

After admitting only the bilateral `0x100 ↔ 0x116` composition, every isolated case matched architecturally. The remaining differences were instruction accounting only.

Measured semantic-only deltas, candidate minus ROM:

### `0x100 -> 0x116`

- first call (`0x164b0`): `-4` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `+14/+13` for countdown 0 (`mode6=0/1`), `-3` with countdown 1

### `0x116 -> 0x100`

- first call (`0x164b0`): `+10` with countdown 0, `-4` with countdown 1
- second call (`0x164c4`): `0/-1` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

Once normalized by the actual `r7/r8` orientation seen by each child, these are exactly the already recovered `0x100 ↔ 0x112` class-3 accounting rules. The existing orientation-sensitive predicate was therefore widened to include `0x116` instead of adding a duplicate formula.

## Validation

With measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- mutable memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `579a745cb4c8e088f6ad1ef577dc4731566b5ece`.

Matrix update commit: `facb5110c6de614e388319a908d883f684483957`.

After promotion, the state-pair matrix contains **56/64 exact pairs**.
