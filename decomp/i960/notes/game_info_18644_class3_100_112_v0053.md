# fa_game_info 0x18644: exact 0x100 ↔ 0x112 recovery (v0053)

## Corridor

- `0x100 = bit8`
- `0x112 = bit8 | bit1 | bit4`
- vector class: `3`
- ROM vector: `(208, 213, 205, 210)`

## Semantic probe

After admitting only the bilateral `0x100 ↔ 0x112` composition, every isolated case matched architecturally. The remaining differences were instruction accounting only.

Measured semantic-only deltas, candidate minus ROM:

### `0x100 -> 0x112`

- first call (`0x164b0`): `-4` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `+14/+13` for countdown 0 (`mode6=0/1`), `-3` with countdown 1

### `0x112 -> 0x100`

- first call (`0x164b0`): `+10` with countdown 0, `-4` with countdown 1
- second call (`0x164c4`): `0/-1` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

The corrections match the already recovered orientation-sensitive class-3 accounting families. The caller's second invocation presents the pair with reversed `r7/r8`, so the accounting block is keyed to the actual child-call orientation.

## Validation

With measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- mutable memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `a4269fdb87f7ba6e1f236666f28fa99b5283f49b`.

Matrix update commit: `56d838a111219368da82d88f1c739ce1bcbc4818`.

After promotion, the state-pair matrix contains **48/64 exact pairs**.
