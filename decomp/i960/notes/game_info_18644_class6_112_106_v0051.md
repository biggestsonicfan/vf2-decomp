# fa_game_info 0x18644: exact 0x112 ↔ 0x106 recovery (v0051)

## Corridor

- `0x112 = bit8 | bit1 | bit4`
- `0x106 = bit8 | bit1 | bit2`
- vector class: `6`
- ROM vector: `(219, 224, 216, 221)`

## Semantic probe

After admitting only the bilateral `0x112 ↔ 0x106` composition, all isolated cases matched architecturally. The remaining differences were instruction accounting only.

Measured semantic-only deltas, candidate minus ROM:

### `0x112 -> 0x106`

- first call (`0x164b0`): `-1` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `0/-1` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

### `0x106 -> 0x112`

- first call (`0x164b0`): `-4` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `+3/+2` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

These deltas are exactly the two class-6 accounting patterns already recovered in neighboring corridors. The caller's second invocation presents the pair in reversed `r7/r8` order, so the correction is keyed to the actual child-call orientation.

## Validation

With measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `cebaa1c456e41c582b9ac34e123c97842aca0023`.

After promotion, the state-pair matrix contains **44/64 exact pairs**.
