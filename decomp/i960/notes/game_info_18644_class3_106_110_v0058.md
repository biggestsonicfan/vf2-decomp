# fa_game_info 0x18644: exact 0x106 ↔ 0x110 recovery (v0058)

## Corridor

- `0x106 = bit8 | bit1 | bit2`
- `0x110 = bit8 | bit4`
- vector class: `3`
- ROM vector: `(208, 213, 205, 210)`

## Semantic probe

After admitting only the bilateral `0x106 ↔ 0x110` composition, every isolated case matched architecturally. The remaining differences were instruction accounting only.

Measured semantic-only deltas, candidate minus ROM:

### `0x106 -> 0x110`

- first call (`0x164b0`): `+7` with countdown 0, `-4` with countdown 1
- second call (`0x164c4`): `+3/+2` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

### `0x110 -> 0x106`

- first call (`0x164b0`): `-1` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `+11/+10` for countdown 0 (`mode6=0/1`), `-3` with countdown 1

Normalized by the actual child `r7/r8` orientation, these are exactly the already recovered `0x102 ↔ 0x114` class-3 accounting corrections. The existing orientation-sensitive predicate was widened to include `0x106 ↔ 0x110`; no new accounting formula was introduced.

## Validation

With measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- mutable memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `c747db4083961de9e8b9be42b0ba2ef92f00220a`.

Matrix update commit: `df7bee10299ff0940475a7d7276601e32ea72be8`.

After promotion, the state-pair matrix contains **58/64 exact pairs**.
