# fa_game_info 0x18644: exact 0x104 ↔ 0x106 recovery (v0061)

## Corridor

- `0x104 = bit8 | bit2`
- `0x106 = bit8 | bit1 | bit2`
- vector class: `7`
- ROM vector: `(221, 226, 197, 202)`

## Semantic probe

After admitting only the bilateral `0x104 ↔ 0x106` composition, all isolated cases matched architecturally. Every non-instruction counter was already exact; only instruction accounting differed.

Measured semantic-only deltas, candidate minus ROM:

### `0x104 -> 0x106`

- first call (`0x164b0`): `-9` without countdown, `-11` with countdown
- second call (`0x164c4`): `+6/+5` without countdown (`mode6=0/1`), `+1` with countdown

### `0x106 -> 0x104`

- first call (`0x164b0`): `+2` without countdown, `0` with countdown
- second call (`0x164c4`): `-5/-6` without countdown (`mode6=0/1`), `-10` with countdown

These deltas are identical to the preceding exact `0x100 ↔ 0x106` class-7 corridor. After normalizing for the actual child orientation, no new formula is needed:

- actual `0x104 -> 0x106` reuses the high class-7 family already used by `0x102 ↔ 0x106` and `0x100 -> 0x106`;
- actual `0x106 -> 0x104` reuses the low bit2-family accounting already used by `0x106 -> 0x100`.

Only the existing orientation-sensitive predicates were widened.

## Validation

With the measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- mutable memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `259eb0fafef4e682d52bad67f8aa756177bb91cb`.

Matrix update commit: `7a007e04d94ced4c97760311a2b18e83ad117745`.

After promotion, the state-pair recovery matrix is **64/64 exact**. Every ordered pair in the measured eight-state matrix is now recovered exactly.
