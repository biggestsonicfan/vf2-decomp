# fa_game_info 0x18644: exact 0x104 ↔ 0x116 recovery (v0056)

## Corridor

- `0x104 = bit8 | bit2`
- `0x116 = bit8 | bit1 | bit2 | bit4`
- vector class: `3`
- ROM vector: `(208, 213, 205, 210)`

## Semantic probe

After admitting only the bilateral `0x104 ↔ 0x116` composition, every isolated case matched architecturally. The remaining differences were instruction accounting only.

Measured semantic-only deltas, candidate minus ROM:

### `0x104 -> 0x116`

- first call (`0x164b0`): `-4` with countdown 0, `-15` with countdown 1
- second call (`0x164c4`): `+14/+13` for countdown 0 (`mode6=0/1`), `-3` with countdown 1

### `0x116 -> 0x104`

- first call (`0x164b0`): `+10` with countdown 0, `-4` with countdown 1
- second call (`0x164c4`): `0/-1` for countdown 0 (`mode6=0/1`), `-14` with countdown 1

These are exactly the already recovered `0x104 ↔ 0x112` class-3 accounting deltas, so the existing orientation-sensitive accounting predicate was widened to include the bit2-preserving `0x104 ↔ 0x116` corridor instead of duplicating a third equivalent formula.

## Validation

With measured accounting applied:

- isolated child checkpoints: **16/16 exact**
- chained native child #1 -> caller ROM -> native child #2 -> tail ROM: **8/8 exact**
- CPU/register architecture: exact
- mutable memory: exact
- instruction/call/return/interrupt counters: zero delta in every tested case
- probe CI build and `ctest`: success

Clean source candidate: `096c367c7457e7c5a37fd56781e2fe331379fc44`.

Matrix update commit: `d64cae4e2c1ad22d9a8555e68d60835a92cce7cb`.

After promotion, the state-pair matrix contains **54/64 exact pairs**.
