#include <stddef.h>

/*
 * TGP implementation placeholder.
 *
 * v0.0.22 reconstructs the ROM regions and proves that the first camera task
 * does not modify geometry RAM. Coprocessor-port offset 0x4000 is currently
 * treated as evidence-bounded arithmetic scratch. The first TGP milestone will
 * describe uploadable microcode and FIFO transactions as typed C structures.
 */
void vf2_tgp_placeholder(void)
{
}
