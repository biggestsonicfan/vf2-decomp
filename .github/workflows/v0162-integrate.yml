name: v0162 integrate

on:
  push:
    branches: [master]
    paths:
      - '.github/v0162.patch'
      - '.github/workflows/v0162-integrate.yml'

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply recovery patch
        run: |
          git apply --recount --index .github/v0162.patch
          git diff --cached --check
      - name: Configure
        run: cmake -S . -B build -DVF2_BUILD_TESTS=ON -DVF2_WARNINGS_AS_ERRORS=ON
      - name: Build
        run: cmake --build build --parallel 2
      - name: Test
        run: ctest --test-dir build --output-on-failure
      - name: Commit recovery
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/v0162.patch .github/workflows/v0162-integrate.yml
          git add include/vf2/native_runtime.h src/recovered/native_runtime.c decomp/i960/notes/scheduler_descriptor_scan_v0162.md
          git commit -m 'Recover scheduler descriptor scan to camera task'
          git push origin master
