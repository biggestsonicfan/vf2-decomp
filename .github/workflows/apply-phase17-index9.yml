name: Apply selector17 index9 ALL INITIALIZE recovery
on:
  push:
    branches: [master]
    paths: [.github/workflows/apply-phase17-index9.yml]
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply recovery
        run: python3 scripts/apply_phase17_index9_all_initialize.py
      - name: Configure
        run: >-
          cmake -S . -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DVF2_BUILD_TESTS=ON
          -DVF2_WARNINGS_AS_ERRORS=ON
      - name: Build
        run: cmake --build build --parallel
      - name: Test
        run: ctest --test-dir build --output-on-failure
      - name: Commit recovery
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm scripts/apply_phase17_index9_all_initialize.py
          git rm .github/workflows/apply-phase17-index9.yml
          git add src/recovered/texture_bridge_match.c decomp/i960/notes/selector17_index9_all_initialize.md
          git commit -m "Recover selector17 index9 ALL INITIALIZE"
          git push
