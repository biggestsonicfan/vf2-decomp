name: v0150 mixed state4 bit6 countdown integration

on:
  push:
    branches: [master]
    paths:
      - '.github/v0150.patch'
      - '.github/workflows/v0150-integrate.yml'

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply v0150 recovery
        run: git apply .github/v0150.patch
      - name: Configure
        run: cmake -S . -B build -DVF2_BUILD_TESTS=ON -DVF2_WARNINGS_AS_ERRORS=ON
      - name: Build
        run: cmake --build build -j2
      - name: Test
        run: ctest --test-dir build --output-on-failure
      - name: Commit recovery
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm -f .github/v0150.patch .github/workflows/v0150-integrate.yml
          git add -A
          git commit -m 'Recover mixed negative state4 bit6 countdown path'
          git push origin HEAD:master
