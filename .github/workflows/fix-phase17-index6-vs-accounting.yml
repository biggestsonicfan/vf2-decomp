name: Fix selector17 index6 VS accounting
on:
  push:
    paths:
      - tools/fix_phase17_index6_vs_accounting.py
      - .github/workflows/fix-phase17-index6-vs-accounting.yml
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply accounting correction
        run: python3 tools/fix_phase17_index6_vs_accounting.py
      - name: Configure strict build
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DVF2_BUILD_TESTS=ON -DVF2_WARNINGS_AS_ERRORS=ON
      - name: Build
        run: cmake --build build --parallel
      - name: Test
        run: ctest --test-dir build --output-on-failure
      - name: Publish verified binary
        uses: actions/upload-artifact@v4
        with:
          name: vf2i960-index6-vs-final
          path: build/vf2i960
      - name: Commit correction
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/recovered/texture_bridge_match.c decomp/i960/notes/selector17_index6_bookkeeping_vs_diagram.md
          git rm -f tools/fix_phase17_index6_vs_accounting.py .github/workflows/fix-phase17-index6-vs-accounting.yml
          git commit -m 'Finalize BOOKKEEPING VS diagram differential'
          git push
