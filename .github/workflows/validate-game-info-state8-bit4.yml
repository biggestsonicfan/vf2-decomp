name: Apply game-info state8 bit4
on: [push]
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply recovery
        run: python .github/scripts/validate_game_info_state8_bit4.py
      - name: Configure strict build
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DVF2_BUILD_TESTS=ON -DVF2_WARNINGS_AS_ERRORS=ON
      - name: Build
        run: cmake --build build --parallel
      - name: Test
        run: ctest --test-dir build --output-on-failure
      - name: Commit recovered corridor and cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/scripts/validate_game_info_state8_bit4.py
          git rm .github/workflows/validate-game-info-state8-bit4.yml
          git add src/recovered/hybrid.c
          git commit -m "Recover fa_game_info state8 bit4 mode6 corridor"
          git push
