name: v0155 compound state4 integration

on:
  push:
    branches: [master]
    paths:
      - '.github/v0155.patch'
      - '.github/workflows/v0155-integrate.yml'

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply v0155 recovery
        run: |
          git apply --index .github/v0155.patch
          git diff --cached --check

      - name: Configure
        run: cmake -S . -B build -DVF2_BUILD_TESTS=ON -DVF2_WARNINGS_AS_ERRORS=ON

      - name: Build
        run: cmake --build build --parallel 2

      - name: Test
        run: ctest --test-dir build --output-on-failure

      - name: Commit recovery
        run: |
          rm .github/v0155.patch .github/workflows/v0155-integrate.yml
          git add -A
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Recover mixed negative state4 compound paths'
          git push origin master
