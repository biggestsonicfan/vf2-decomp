name: Export v0143 worktree

on:
  push:
    branches: [master]
    paths:
      - .github/workflows/export-v0143-worktree.yml

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: vf2-v0143-worktree
          path: .
          include-hidden-files: true
