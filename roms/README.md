# ROM directory

Place the **contents** of your legally obtained `vf2.zip` set here:

```text
roms/vf2/
  epr-18385.12
  epr-18386.13
  ...
```

Then run:

```sh
vf2rom verify roms/vf2
vf2rom info roms/vf2
```

ROM files and archives are excluded by `.gitignore`.
